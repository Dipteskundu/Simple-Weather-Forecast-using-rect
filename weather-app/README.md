# Weather Command Center

Weather Command Center is a React (Vite) weather dashboard that combines:

- **Live weather telemetry** from the **OpenWeatherMap API**
- A computed **Environmental Comfort Score (0-100)**
- Optional **AI Concierge** lifestyle guidance via the **Gemini API** (structured JSON)

## Live Link

- https://react-weather-forcasting.netlify.app/

## What It Does

- Search a city and fetch current weather (metric units)
- Display temperature, feels-like, humidity, wind, pressure, visibility
- Calculate a comfort score from humidity + wind
- Generate 4 advisory cards (Outfit, Avoid, Hydration, Skincare) via Gemini (or fall back to templates if Gemini is not configured)

## Tech Stack

- React + Vite
- OpenWeatherMap API
- Gemini API (optional)

## Run Locally

### Prerequisites

- Node.js 18+ (recommended)
- npm

### 1) Clone

```bash
git clone <your-repo-url>
cd Simple-Weather-Forecast-using-rect
```

### 2) Install

From the repo root:

```bash
npm install
```

This will also install dependencies inside `weather-app/`.

Alternatively, you can work directly inside the app folder:

```bash
cd weather-app
npm install
npm run dev
```

### 3) Configure Environment Variables

Create `weather-app/.env` (it is auto-created from `weather-app/.env.example` on install if missing) and set your keys:

```env
VITE_WEATHER_API_KEY=YOUR_OPENWEATHERMAP_KEY
VITE_GEMINI_API_KEY=YOUR_GEMINI_KEY
```

Notes:
- `VITE_GEMINI_API_KEY` is optional. If missing, the app uses a safe fallback report template.

### 4) Start Dev Server

From the repo root:

```bash
npm run dev
```

Then open:
- http://localhost:5173

### Useful Scripts

From the repo root:

```bash
npm run lint
npm run build
npm run preview
```

## Contributing

Contributions are welcome.

1. Fork the repo
2. Create a feature branch: `git checkout -b feature/my-change`
3. Make changes (keep code style consistent)
4. Verify locally:
   - `npm run lint`
   - `npm run build`
5. Open a Pull Request with a clear description and screenshots (if UI changes)

---

## Architecture & Implementation Plan (Notes)

Weather Command Center: Step-by-Step Implementation Plan

This guide outlines how to build a fully functional Weather Command Center in React. It combines real-time data from the OpenWeatherMap API with hyper-personalized lifestyle diagnostics generated by the Gemini API.

Architecture Overview

The application consists of three primary layers:

State Management & Data Retrieval (Main App Component): Fetches climate data from OpenWeatherMap and triggers the secondary LLM analysis.

Meteorological Core (WeatherStatsPanel): Displays calculated comfort ratings, temperatures, humidity, wind speed, pressure, and visibility.

AI Concierge (ConciergeDashboard): Translates complex atmospheric telemetry into 4 structured, easy-to-read advisory cards.

       ┌────────────────────────┐
       │   React App Wrapper    │
       └───────────┬────────────┘
                   ▼
       ┌────────────────────────┐
       │ OpenWeatherMap Fetch   │
       └───────────┬────────────┘
                   │ (Temperature, Humidity, Wind, Pressure)
                   ▼
       ┌────────────────────────┐       ┌────────────────────────┐
       │   WeatherStatsPanel    │ ◄───► │    Gemini LLM Call     │
       └────────────────────────┘       └───────────┬────────────┘
                                                    │ (Structured JSON)
                                                    ▼
                                        ┌────────────────────────┐
                                        │  4 Concierge Cards     │
                                        │  - Outfit & Styling     │
                                        │  - Adverse Exposure    │
                                        │  - Dynamic Hydration   │
                                        │  - Dermal Protection   │
                                        └────────────────────────┘


Step 1: Initialize State & Setup Search Events

In your main functional component (usually App.jsx or your layout module), establish the state variables required to hold weather metrics, input fields, loading animations, and AI-generated advice.

Required State Variables

const [city, setCity] = useState('');
const [weatherData, setWeatherData] = useState(null);
const [error, setError] = useState('');
const [isLoading, setIsLoading] = useState(false);
const [reportData, setReportData] = useState(null);
const [isLlmLoading, setIsLlmLoading] = useState(false);


Input & Keyboard Handlers

Bind the onChange event of the search <input> to setCity(e.target.value).

Implement a onKeyPress (or standard onKeyDown) handler to allow searching instantly when pressing Enter:

const handleKeyPress = (e) => {
  if (e.key === 'Enter') {
    fetchWeather();
  }
};


Step 2: Fetch and Calculate Weather Telemetry

When the user submits a city, execute an asynchronous request to the OpenWeatherMap API using your key. Once returned, compute an Environmental Comfort Score combining wind and humidity levels.

Fetch Function Implementation

Environment Variables (Vite)

Create `weather-app/.env` (never commit this file):

VITE_WEATHER_API_KEY=your_openweathermap_key_here
VITE_GEMINI_API_KEY=your_gemini_api_key_here

Then access keys in code via `import.meta.env`.

const API_KEY = import.meta.env.VITE_WEATHER_API_KEY;

const fetchWeather = async () => {
  if (city.trim() === '') {
    setError('Please enter a city name.');
    return;
  }

  setIsLoading(true);
  setError('');
  setReportData(null); // Reset previous reports

  try {
    const response = await fetch(
      `https://api.openweathermap.org/data/2.5/weather?q=${city}&appid=${API_KEY}&units=metric`
    );
    if (!response.ok) throw new Error('City not found. Please try again.');
    
    const data = await response.json();
    
    // Formula to calculate an Environmental Comfort Score (1-100)
    // Absolute deviation from comfortable humidity (around 45%)
    const humidityImpact = Math.max(0, 100 - (Math.abs(data.main.humidity - 45) * 1.8));
    // High wind speed reduces outdoor comfort
    const windImpact = Math.max(0, 100 - (data.wind.speed * 4.5));
    const comfortScore = Math.round((humidityImpact * 0.6) + (windImpact * 0.4));

    setWeatherData({
      city: data.name,
      country: data.sys.country,
      temp: Math.round(data.main.temp),
      feelsLike: Math.round(data.main.feels_like),
      humidity: data.main.humidity,
      wind: Math.round(data.wind.speed * 3.6), // Convert m/s to km/h
      pressure: data.main.pressure,
      visibility: (data.visibility / 1000).toFixed(1), // Convert meters to km
      condition: data.weather[0].main,
      description: data.weather[0].description,
      comfortScore,
    });
  } catch (err) {
    setWeatherData(null);
    setError(err.message);
  } finally {
    setIsLoading(false);
  }
};


Step 3: Implement Gemini API with Structured JSON Outputs

To render the premium grid cards properly, we must instruct Gemini to respond with a strict JSON schema instead of plain Markdown. This allows the React component to directly extract the text strings for each card cleanly.

Setup the API Call Inside useEffect

Whenever weatherData updates, invoke a useEffect hook to send the metrics to Gemini.

useEffect(() => {
  if (!weatherData) return;

  const fetchReport = async () => {
    setIsLlmLoading(true);
    
    // Customize your prompt to inject the current metrics
    const prompt = `Act as an elite personal health, style, and skin advisor.
    Analyze the current weather data for ${weatherData.city}:
    - Temperature: ${weatherData.temp}°C
    - Feels Like: ${weatherData.feelsLike}°C
    - Humidity: ${weatherData.humidity}%
    - Wind speed: ${weatherData.wind} km/h
    - Environmental Comfort Score: ${weatherData.comfortScore}/100

    You must respond strictly with a valid JSON object matching the requested schema. No conversational preamble.

    Explain how the metrics (specifically quoting humidity, temperature, and wind) affect the advice.`;

    const geminiApiKey = import.meta.env.VITE_GEMINI_API_KEY;

    try {
      const response = await fetch(
        `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-09-2025:generateContent?key=${geminiApiKey}`,
        {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ 
            contents: [{ parts: [{ text: prompt }] }],
            generationConfig: {
              responseMimeType: "application/json",
              responseSchema: {
                type: "OBJECT",
                properties: {
                  outfit: { type: "STRING" },
                  avoid: { type: "STRING" },
                  hydration: { type: "STRING" },
                  skincare: { type: "STRING" }
                },
                required: ["outfit", "avoid", "hydration", "skincare"]
              }
            }
          })
        }
      );
      
      const result = await response.json();
      const textResponse = result?.candidates?.[0]?.content?.parts?.[0]?.text || "{}";
      const parsedData = JSON.parse(textResponse.trim());
      setReportData(parsedData);
    } catch (err) {
      console.error("Gemini Parse Error: Falling back to template compiler", err);
      // Ensure the user gets tailored metrics even if the LLM request fails
      setReportData({
        outfit: `Today's temperature is ${weatherData.temp}°C and the humidity is ${weatherData.humidity}%. We recommend lightweight, breathable white cotton clothing to maintain maximum comfort and body temperature regulation.`,
        avoid: `Due to a Comfort Score of ${weatherData.comfortScore}/100, avoid heavy cardiovascular workouts in direct sunlight. Keep active hours early or late in the day.`,
        hydration: `With humidity at ${weatherData.humidity}% and a feels-like temperature of ${weatherData.feelsLike}°C, ensure you consume at least 250ml of cool water every hour.`,
        skincare: `To combat solar exposure at ${weatherData.temp}°C, apply SPF 50 sunscreen. Wear protective sunglasses and stay in shaded areas whenever possible.`
      });
    } finally {
      setIsLlmLoading(false);
    }
  };

  fetchReport();
}, [weatherData]);


Step 4: Component Architecture & Layout

To keep your code modular and readable, subdivide your markup into clear structural blocks.

1. Main Search & Header (App)

Renders the title banner, search input with magnifying glass action button, and conditional loaders.

2. Meteorological Panel (WeatherStatsPanel)

Displays a visual breakdown of raw data in a nice card layout:

City Name and Country Tag.

Giant temperature display + feels-like reading.

4-item grid (Humidity, Wind Velocity, Atmospheric Pressure, Visibility).

Comfort meter block showing the computed rating.

3. Advisory Grid (ConciergeDashboard)

Rendered on the right side of the screen once loaded. Uses a grid containing 4 cards matching the specific style identifiers in your UI layout:

<div className="concierge-grid">
  {/* Outfit & Styling */}
  <div className="concierge-card card-style">
    <div className="card-header">
      <span className="card-icon">👕</span>
      <h3>Outfit & Styling</h3>
    </div>
    <div className="card-body">{reportData.outfit}</div>
  </div>

  {/* Adverse Exposure */}
  <div className="concierge-card card-avoid">
    <div className="card-header">
      <span className="card-icon">🚫</span>
      <h3>Adverse Exposure</h3>
    </div>
    <div className="card-body">{reportData.avoid}</div>
  </div>

  {/* Dynamic Hydration */}
  <div className="concierge-card card-hydration">
    <div className="card-header">
      <span className="card-icon">💧</span>
      <h3>Dynamic Hydration</h3>
    </div>
    <div className="card-body">{reportData.hydration}</div>
  </div>

  {/* Dermal Protection */}
  <div className="concierge-card card-skincare">
    <div className="card-header">
      <span className="card-icon">🧴</span>
      <h3>Dermal Protection</h3>
    </div>
    <div className="card-body">{reportData.skincare}</div>
  </div>
</div>


Step 5: Responsive Layout Style Guide (CSS)

Use these layout standards to ensure your application works across mobile, tablet, and desktop viewports.

Responsive Grids: Use grid-template-columns: 1fr; as the default for mobile viewports, and expand to grid-template-columns: 1.2fr 1.8fr; inside a @media (min-width: 768px) block for desktop.

Color Borders: Accentuate your cards with dynamic colored left-borders (border-left: 4px solid var(--accent-color)) so each topic category (Style, Avoid, Hydration, Skin) is quickly recognizable.

Obsolete Tag Mitigation: Use span or standard division components (div) to display dynamic variables, avoiding browser console warning prompts.
